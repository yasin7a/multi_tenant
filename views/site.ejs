<div class="card site-hero">
  <div class="site-avatar"><%= user.username.charAt(0).toUpperCase() %></div>
  <span class="badge"><%= user.tenant.subdomain %></span>
  <h1><%= user.username %></h1>
  <p class="tagline"><%= user.tenant.name %></p>
  <div class="divider"></div>
  <p class="meta">Welcome to my tenant site.</p>
</div>
